name: Build Android APK

on:
  workflow_dispatch: {}          # 手动点击运行
  push:
    tags: ['v*']                 # 打 tag(如 v1.0)自动出包

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with: { node-version: '20' }

      - name: Setup Java
        uses: actions/setup-java@v4
        with: { distribution: 'temurin', java-version: '17' }

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install deps
        run: npm install

      - name: Add & sync Android
        run: |
          npx cap add android || true
          npx cap sync android

      - name: Build Debug APK (可直接安装)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Rename APK
        run: |
          VER="${GITHUB_REF_NAME}"
          case "$VER" in refs/*|main) VER="dev-$(date +%Y%m%d)";; esac
          cp android/app/build/outputs/apk/debug/app-debug.apk "千词万斩-${VER}.apk"
          echo "APK_NAME=千词万斩-${VER}.apk" >> $GITHUB_ENV

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: 千词万斩-APK
          path: ${{ env.APK_NAME }}

      - name: Publish to Release (仅打 tag 时)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.APK_NAME }}
